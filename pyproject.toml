[project]
name = "us-state-combiner"
version = "0.1.0"
description = "Project to combine US States"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mapnik>=4.2.2",
    "pandas>=3.0.0",
    "pycairo>=1.29.0",
]
